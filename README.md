# LLM Document QA & Abbreviation Extractor

This Streamlit app lets you:

- Ask questions about uploaded documents (PDF, DOCX, TXT, HTML)
- Extract abbreviation → full-term pairs using an LLM
- Run directly in the browser via Streamlit Community Cloud

## Live demo

https://<your-app-name>.streamlit.app

## How to run locally

```bash
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>
pip install -r requirements.txt
streamlit run app.py
